apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: excalidraw-cert
  namespace: excalidraw
spec:
  secretName: excalidraw-tls-secret
  issuerRef:
    name: clusterissuer
    kind: ClusterIssuer
  dnsNames:
    - excalidraw.lan