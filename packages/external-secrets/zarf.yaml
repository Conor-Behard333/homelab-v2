kind: ZarfPackageConfig
metadata:
  name: external-secrets
  description: deploy external-secrets
    
components:
  - name: external-secrets
    required: true
    charts:
      - name: external-secrets
        namespace: external-secrets
        version: 0.19.2
        url: https://charts.external-secrets.io
        valuesFiles:
          - "../../src/external-secrets/values/values.yaml"